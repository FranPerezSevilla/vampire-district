# Ordinary buildings: night-city material kit

## Bounded implementation

### Full West Market art kit and classic projection — 2026-09-20

The user approved extending the new quiet materials/roof language to the reorganized neighborhood and removing the artificial frontal perspective. All eleven West Market volumes now opt into shared profiles. Three additional authored elevations distinguish courtyard homes, the former warehouse and the workshop; narrow wings sample the central bays, wider buildings repeat side bays around one centered entrance. Primary facade and dormer orientation follow the existing entrance data. Roof equipment leaves dormer footprints clear. Physical footprints, storey counts, streets and population are unchanged by this pass.

CityPerspective again projects radially away from the camera center, with no frontal bias or landmark proximity adjustment. North–south and east–west controls range 0–2000%, default 100% classic strength, and update roof/props/light projection together. Cars retain their separate control. Visibility margins track selected gains, not the maximum possible debug setting. Full implementation scope, costs and validation: [WEST_MARKET_ART_AND_PARALLAX.md](tasks/WEST_MARKET_ART_AND_PARALLAX.md). Production build, 59 focused tests and neighborhood/control browser review pass; the broader affected runner is still blocked by its Windows npm child-launch issue. Earlier sections describe historical iterations.

### Quiet dark materials — 2026-09-20

User approves reducing the noisy texture and increasing quiet near-black masses in the same three-building trial. Authority: OrdinaryBlockArchitecture composes facades, OrdinaryBlockRoofs maps the authored roof atlas, GameScene preloads their sources. Scope: sibling v2 facade/roof image assets, matching loader/key references and crop adjustments only if needed, asset provenance and this handoff. Keep existing 4/2/3 floors, silhouettes, physical geometry, parallax, lights, window/door locations, traffic and city topology. Acceptance: visibly reduce wall/roof microcontrast while retaining crisp openings, warm-light hierarchy and readable plane boundaries at game scale; no extra per-frame pass, texture count or mesh objects; inspect actual game views and compare resident material/memory counts. Non-goals: citywide rollout, global exposure/lighting change, new geometry, gameplay or performance-system refactor. Imagegen edits authored source sheets; no procedural texture replacement. Existing focused checks plus production-browser review suffice for this material-only revision.

Implemented: built-in imagegen edits saved as `ordinary-block-v2.png` and `ordinary-block-roofs-v2.png`. Walls lose their high-contrast mottling, stone trim becomes quieter, slate/zinc joints are subdued, warm panes and entrances stay crisp and bright. Layout and source sizes match v1, so crops/geometry are unchanged. The flat-terrace vertex tint changes from 0x58616f to 0x9aa6b6 because v2 already has darker material values; this preserves a readable charcoal surface instead of compounding the old darkening. Two active atlases replace two active atlases; v1 stays on disk but is not preloaded. No filters or extra draw pass.

Validation: production build + 32 existing focused tests pass. Production browser reviews street/tenement/market/shop views with no JS errors. Before/after composed-material bytes match exactly at all four positions (19,279,344 / 20,574,832 / 22,416,112 / 24,057,712); seven roof parts and five railings remain unchanged. Source dimensions remain 1086×1448 and 1254×1254 (12,580,176 decoded RGBA bytes total). Combined source PNG size falls from 6,152,710 to 3,820,127 bytes, about 38%; this reduces the asset payload, not GPU memory or per-frame draw cost. No FPS increase is claimed. Affected plan reviewed and runner attempted; Windows npm child still exits before the full selected suite. The full branch suite remains outstanding.

Evidence: `screenshots/ordinary-quiet-before-tenement.png`, `ordinary-quiet-tenement.png`, `ordinary-quiet-market.png`, `ordinary-quiet-shops.png`, `ordinary-quiet-before-review.json` and `ordinary-quiet-review.json`. Exact prompts and generated source paths live in the architecture asset README. Preview updated; no commit/push. Scope remains West Market pending visual feedback.

### Roofs and height rhythm — 2026-09-20

User accepts the three architectural fronts and authorizes the proposed next step: tenement four storeys, market two, shops three. Scope: these same three buildings; physically sized repeated upper storeys, authored dark slate/zinc/terrace surfaces, a raised mansard perimeter with clear central terrace, a market skylight and a shop terrace/access. WorldScale remains the height authority and reads shared OrdinaryBlockProfiles presentation metadata, including for streamed copies; explicit building height/storey overrides take precedence. BuildingParallax owns projected roof surfaces and resource lifetime; RooftopObjects keeps individual equipment attached to the correct roof elevation. New assets use imagegen, not procedural texture painting.

Expected files: scoped building metadata/composition, OrdinaryBlockArchitecture, a reusable OrdinaryBlockRoofs module, existing roof/equipment/material integration, GameScene preload, asset README and focused tests. Acceptance: 4/2/3 visible floors at unchanged storey and entrance scale; distinct roof silhouettes, coherent seams in all four camera directions and sliders; central roof areas and equipment remain clear of sloping rims/skylight; no per-frame texture generation; real-game screenshots and bounded performance check. Non-goals: changing roads/footprints/colliders, citywide restyle, cars/characters, new roof-access or jumping mechanics, landmark treatment, camera settings or full building sprite stacking.

Implemented: shared profiles select four, two and three 3.4 m storeys (13.6 / 6.8 / 10.2 m facade height). Original door/window proportions are preserved by repeating upper-storey art, rather than stretching the full elevation. A sign crossing the source atlas midline is masked with its own masonry on added shop floors, so AFTER HOURS appears once. Front material height cap is 768 px; width cap remains 1024 px.

New authored `ordinary-block-roofs-v1.png` supplies six shared surfaces (1254 square; 3,282,314 bytes on disk, 6,290,064 decoded bytes). Tenement: slate mansard perimeter, two independent zinc dormers, clear central terrace. Market: shallow zinc rim and pitched glazed skylight. Shops: iron-railed terrace and separate access enclosure. Existing equipment is supported at the terrace elevation and excluded from slopes/skylight/access. Seven roof mesh objects and five existing fence-stack instances cover the whole trial; surfaces share the atlas. Static geometry is prepared once, flat planes avoid unnecessary subdivisions, offscreen/back-facing quads are culled. Dormant entries destroy meshes and railings with the existing 12-entry / 32 MiB inactive composed-material budget. That budget is not a total active-scene/source-atlas memory limit.

Validation: production build and 35 focused tests passed (scale/rows, roof bounds, equipment clearance, identical eave projection across sliders, resource disposal, existing material/projection/lighting checks). Browser: three roof overviews, frontal/lateral/rear extremes, flat view, hospital/police/cathedral roundtrip, no JS errors and no orphaned roof objects. Repeated camera route kept material serial 63 -> 63. Warm motion median 16.7 ms / p95 33.3 ms, two intervals over 33.4 ms; not stutter-free. Isolated same-session roof-draw A/B (two enabled, two disabled samples): enabled 58.06 / 57.69 FPS, disabled 57.50 / 59.60 FPS, all p95 ~16.8 ms. Roof meshes plus railings cost ~0.17–0.22 ms/frame of measured CPU submission time; this does not measure isolated GPU cost or guarantee 60 FPS citywide. Affected runner was attempted but still cannot launch its npm child in this Windows environment; full branch suite remains outstanding.

Evidence: `screenshots/ordinary-roofs-tenement.png`, `ordinary-roofs-market.png`, `ordinary-roofs-shops.png`, `ordinary-roofs-rear.png`, `ordinary-roofs-flat.png`, `ordinary-roofs-review.json`, `ordinary-roofs-cost.json`. This is an architectural presentation pass with space reserved for later rooftop gameplay; new access/collision/actor depth mechanics are not implemented. Preview updated at port 4174; no commit/push.

### Architectural character prototype — 2026-09-20

User authorizes a trial in the existing review block. Goal: readable evidence of former wealth and 1990s street appropriation, beyond changing wall texture. Scope is tenementNorth, marketBlock and shops only: authored residential, arcade-market and late-night shop fronts; restrained shallow facade relief with the existing shared projection; warm light accents. OrdinaryBuildingMaterials remains the composition authority; BuildingParallax remains the only projection/residency owner. Expected files: an authored architecture atlas and README, a reusable OrdinaryBlockArchitecture recipe module, the two existing material/projection modules, GameScene preload, focused geometry tests and real-game screenshots.

Acceptance: inspect all three fronts at normal gameplay scale and oblique/extreme slider views; distinctive entrances and vertical bays; no floating seams at roof/ground or displaced facade textures; no new canvas composition while moving within resident blocks; limited extra geometry and measured same-session traversal. Non-goals: no citywide rollout, collider/road/height changes, landmark changes, actor/car changes, new physics objects, camera behaviour or extra gameplay loops. Reuse authored raster art, no procedural surface painting. Save the prompt and keep v2 outside this block.

Implemented: `ordinary-block-v1.png` supplies three distinct two-storey elevations (tenement portal/balconies, market arcades, AFTER HOURS shop), plus matching secondary wall modules. Presentation recipes choose crops, warm light positions and shallow beveled pier sections. The front is a continuous textured surface, with up to 24 cm of relief that closes flush at the existing footprint and coping. Optional cached panels in MaterialQuad share one texture/batch/object per height band; they do not multiply sprites by number of piers. The three ids form the entire trial. `architectureKit` may explicitly choose a recipe or false to opt out; substantially different heights/proportions need new art recipes. Doors and shutters in this pass are presentation only.

Validation on 2026-09-20: production build and 34 focused native tests passed, including shared UV spans, offscreen culling, single texture binding, ground/roof seams and camera continuity. Production browser boot, all three fronts, rear, frontal/lateral/rear extremes, reset and traversal to hospital/police/cathedral had no JS errors. Roof equipment remained attached (max error ~2.54e-13); no orphaned objects. Repeated camera traversal reused material serial 59 -> 59. Warm traversal median 16.7 ms / p95 16.8 ms, with one frame >33.4 ms among 179 measured intervals. A brief same-session stationary A/B measured 59.52 -> 59.24 FPS (both median/p95 ~16.7 ms, no interval >33.4 ms), not evidence of a significant change nor a citywide 60 FPS guarantee. Composed material bytes are identical at the three positions; scene objects 786 -> 792. New shared atlas costs 6,290,112 decoded bytes (~6 MiB), 2,870,396 bytes on disk. These source bytes are additional to the resident material cache. Affected plan was reviewed and runner attempted; its Windows npm child exits before the full selected suite, so full branch validation is still outstanding.

Evidence: `screenshots/ordinary-block-tenement.png`, `ordinary-block-market.png`, `ordinary-block-front-220.png`, `ordinary-block-before-review.json`, `ordinary-block-after-review.json` and `ordinary-block-controls-review.json`. Local preview serves the new production build at port 4174. No commit/push performed. Next step: user visual comparison before any wider rollout.

### Material revision after resume — 2026-09-19

User feedback: fine red-brick v1 facade texture clashes with the ground, cars and existing art. Scope: an authored v2 facade atlas with larger quiet soot-grey surfaces, controlled relief and simpler interiors; only material selection/preload and material composition if needed. Authority remains OrdinaryBuildingMaterials / BuildingMaterialImages. Preserve building footprints, geometry, heights, roofs/equipment, car/actor art, projection and sliders. Acceptance: inspect the actual same block at normal scale, compare to pavement and vehicles, check loading and unchanged material/object counts. Reuse the existing eight-cell atlas contract; no extra per-frame cost or extra simultaneous atlas.

Implemented: `ordinary-facades-v2.png` uses smoky dark render/stone, plain lintels and simple warm interiors. v1 is retained on disk but not loaded; only the facade material changes. Five-pixel source insets exclude dark sheet separators. Reviewed native game screenshots of housing and commercial fronts alongside existing paving and traffic: `screenshots/ordinary-buildings-v2.png` and `screenshots/ordinary-buildings-v2-market.png`. This is an alternative awaiting the user's visual feedback, not an approved final city style.

Validation: production build and 30 focused native tests pass; the three-view production-browser review has no JS errors. Material bytes match the earlier v1 review exactly at each camera location (18,004,224 at tenement; 19,139,904 at market/rear), same 786 scene objects in the final sample, three ordinary atlases loaded and v1 facade absent. This resumed session's short headless frame sample measured median 33.3 ms / p95 33.4 ms, unlike the previous session's 16.7 ms; no same-session controlled baseline was taken, so no FPS improvement or unchanged citywide performance is claimed. Detailed values: `screenshots/ordinary-buildings-v2-review.json`. Affected plan reviewed and runner attempted; its Windows npm child still exits before running the selected full suite. Native focused checks were run directly; full-suite validation remains outstanding.

Authority: BuildingParallax owns the existing roof/facade projection; BuildingMaterialImages owns resident material composition and disposal. Add an OrdinaryBuildingMaterials kit for ordinary housing, commercial and industrial volumes using authored raster modules.

Files: new facade/roof image atlases in phaser/assets/architecture, OrdinaryBuildingMaterials.js, BuildingMaterialImages.js, BuildingParallax.js, GameScene preload, focused tests and this document.

Acceptance: a representative block around tenementNorth / shops / marketBlock reads as dark masonry, recessed crisp windows with selective warm interiors, inhabited ground floors and articulated roofs. Modules retain consistent world scale and fit narrow buildings; materials are composed only at cache creation, retained/released with the existing budget. Inspect real-game screenshots and measure a camera traversal before/after.

Non-goals: no new building simulation, collisions, city footprints, storey heights, landmark art, car/actor changes, global light engine or full-building sprite stacking. Authored atlases provide visual detail; code only lays out/crops reusable modules on existing surfaces.

User addition: rooftop equipment must be independent objects with roof-relative bases, their own height, material faces and the shared city projection, ready for subsequent rooftop gameplay. RooftopObjects owns the visual modules and BuildingParallax owns their residency/disposal. Do not bake equipment into the roof. Add frontal/lateral/rear live sliders to the existing debug perspective panel; CityPerspective remains the single projection authority for buildings, rooftop modules and street props. Cars remain independent. Preserve continuous, shared affine projection at every attachment seam. Sliders must update a stationary scene, isolate game controls, reset and clean up on shutdown. Validate roof attachment vertices at extremes and resource reuse during movement.

## Validation

Use affected plan/runner; focused native geometry/cache tests plus production browser boot, representative block, side/rear facades, narrow buildings, camera traversal, atlas memory and cache cleanup. Existing broad-branch failures remain separately reported.

## Assets and prompts

Built-in image_gen through the imagegen skill. Three original PNGs live in `phaser/assets/architecture`; full prompts, cell layout and crop notes are recorded in that directory's README.

## Implemented layout

Ordinary housing, shops/markets and industrial buildings share this kit. Landmark campuses, skyline towers and their authored attachments retain their existing art. After the West Market layout pilot, the review buildings are `tenementNorth` (640,1280), `shops` (870,1330), `marketBlock` (264,1280); stand at (818,1524) for the residential/shop frontage or (347,1538) for the market square. See `tasks/WEST_MARKET_LAYOUT.md` for geometry, entrances and traversal.

Facade bays use the existing storey height in world units. There is one ground-floor entrance/storefront per front, blank masonry between some upper bays, and sparse warm interiors. Facade art is already exposed for night: skip the old second darkening pass. A shared soft light stamp adds spill during cache composition; window geometry stays sharp. Roofs use a repeated quiet membrane with fixed-width nine-sliced stone coping. No equipment is baked onto these roofs.

### Rooftop objects

`RooftopObjects.js` defines ventilation units, chimneys and access enclosures as independent scene objects. Each has a stable id, building id, world footprint, roof elevation (`base`) and physical height. Its top and upright faces sample one shared authored atlas. Five surfaces per object, back-face culling, no sprite slices or per-object canvas/texture allocation. The access door appears only on its front. Foot vertices use exactly the same height-plane transform as the host roof.

An ordinary building may override the reusable placements with `roofObjects: [{id, kind, u, v, width, depth, height}]`; u/v are normalized roof positions and dimensions are metres. `roofObjects: []` leaves a clear roof. Placement keeps a parapet margin and rejects overlapping equipment. This is presentation metadata for future rooftop encounters; equipment collision, cover, interaction and roof-layer gameplay are not implemented by this art pass.

BuildingParallax controls active/dormant residency and destroys equipment when its host cache is evicted. Existing dormant limits remain 12 buildings / 32 MiB of composed materials. Source atlases are shared, approximately 18 MiB decoded total (three 1254-square textures).

### Perspective controls

The lower-left debug panel adds Frontal / Lateral / Trasero from 0–250%, plus the independent car volume control and a reset button. Changes apply live without moving the camera. 100% preserves the previous city view; all three at 0% give the orthographic footprint. Opposing north/south projection anchors preserve one affine field, so attached roofs, walls, equipment and street furniture share their projected vertices. The rear anchor stays behind the zero-lean point even at close zoom. No landmark approach trigger is restored. Cars retain their own projection.
