# TODO

## Immediate

- Commit the current `index.html` prototype into the repository.
- Keep the project runnable by opening `index.html` directly in a browser.
- Avoid external assets until the core loop is stable.

## Near-term polish

- Tune procedural audio volume and timing.
- Improve shadow readability without making the whole map too bright.
- Balance police/hunter pursuit pressure.
- Improve body hiding distribution and route clarity.
- Keep the journalist mission readable from start to finish.

## Later

- Optional sprite support behind `FEATURES.useSprites = false`.
- Optional base64 embedded sprites only when the visual direction stabilizes.
- Separate repo structure only if the single-file workflow becomes too painful.

## Design constraints

- Single HTML file preferred.
- Vanilla JavaScript only.
- No required external assets.
- Must be easy to upload/download and iterate from ChatGPT on any PC.
