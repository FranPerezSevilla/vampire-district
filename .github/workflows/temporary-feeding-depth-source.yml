name: Temporary feeding-depth source snapshot

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
      - name: Archive tracked source
        run: git archive --format=zip --output=viceblood-feeding-depth-source.zip HEAD
      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: viceblood-feeding-depth-source
          path: viceblood-feeding-depth-source.zip
          retention-days: 2
