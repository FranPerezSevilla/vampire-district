name: Temporary Heat Exposure source snapshot

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
      - name: Archive tracked source
        run: git archive --format=zip --output=viceblood-heat-exposure-source.zip HEAD
      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: viceblood-heat-exposure-source
          path: viceblood-heat-exposure-source.zip
          retention-days: 2
